apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: mysecret
  namespace: webapp-prod
spec:
  encryptedData:
    foo: {{ .Values.sealedsecrets.mysecret.foo }}
  template:
    metadata:
      name: mysecret
      namespace: webapp-prod
